{
  "name": "@happy-wakey/interfaces-contracts",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "format:check": "tsp format --check typespec/*.tsp",
    "generate:typespec": "tsp compile typespec/main.tsp --output-dir generated/typespec && tsp compile typespec/protobuf.tsp --output-dir generated/typespec/protobuf --emit @typespec/protobuf",
    "generate": "npm run generate:typespec && node scripts/generate_contracts.mjs",
    "verify:generated": "node scripts/verify_generated.mjs",
    "check": "npm run format:check && npm run generate && npm run verify:generated && git diff --exit-code -- generated"
  },
  "devDependencies": {
    "@typespec/compiler": "1.15.0",
    "@typespec/http": "1.15.0",
    "@typespec/openapi3": "1.15.0",
    "@typespec/protobuf": "0.85.0",
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "openapi-typescript": "7.10.1",
    "yaml": "2.9.0"
  }
}
